import styled from "styled-components";

export const Wrapper = styled.section`
  width: 100%;
  min-width: 1000px;
  height: fit-content;
  display: flex;
  flex-direction: column;
  gap: 1rem;

  > p {
    text-align: justify;
  }

  > h1 {
    align-self: center;
  }
`
